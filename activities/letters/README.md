# تحدي الحروف

نشاط تنافسي بين الفرق: يختار الفريق حرفًا من اللوحة، فيظهر سؤال إجابته
تبدأ بذلك الحرف. الحرف يتلوّن بلون الفريق عند الإجابة الصحيحة، ويُغلق
بالرمادي عند الخطأ.

## الحالة

placeholder. الهيكل فقط، بلا كود. النسخة العاملة الحالية موجودة خارج
هذا المجلد ولم تُنقل ولم تُعدَّل.

## الملفات

| الملف | الوظيفة |
|---|---|
| `index.html` | صفحة النشاط |
| `style.css` | أنماط النشاط فقط |
| `app.js` | منطق النشاط |
| `config.json` | إعدادات النشاط |
| `README.md` | هذا الملف |
